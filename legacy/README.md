# Legacy container definitions

These files are retained only as migration history and rollback reference.
They are disabled and must not be used for production deployment.
Production ownership is `/etc/containers/systemd/hugo-onepager.container` and deployment runs through `../deploy-quadlet.sh`.
